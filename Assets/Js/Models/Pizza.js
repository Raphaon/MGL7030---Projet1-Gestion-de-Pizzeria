// La class de pizza va nous permettre de gerer les pizzas et la facturation 

export class Pizza {

                constructor(id, taille, prix, ingredients) {
                this.id = id;
                this.taille = taille;
                this.prix = prix;
                this.ingredients = ingredients;
        }

}
